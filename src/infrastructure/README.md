# infrastructure

Adapters for other systems. Holds: the git adapter, the tracker adapter, the agent adapter and
the transcript sinks.

A worktree is a git object, not a directory: worktree operations belong to the git port.

See `docs/agents/layers.md`.
