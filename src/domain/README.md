# domain

Pure rules and the driven ports those rules need. No I/O, and it imports no layer.

Holds: the manifest and its schema; the decision function; slate membership and ordering; status
derivation; transitive skip; branch naming; squash-body composition; the post-implementer
assertions; agent prompt composition; and every driven port.

See `docs/agents/layers.md`.
